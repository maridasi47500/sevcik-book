<%= form_with(model: bow_portion) do |form| %>
  <% if bow_portion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bow_portion.errors.count, "error") %> prohibited this bow_portion from being saved:</h2>

      <ul>
        <% bow_portion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
