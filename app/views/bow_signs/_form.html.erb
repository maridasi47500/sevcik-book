<%= form_with(model: bow_sign) do |form| %>
  <% if bow_sign.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(bow_sign.errors.count, "error") %> prohibited this bow_sign from being saved:</h2>

      <ul>
        <% bow_sign.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
