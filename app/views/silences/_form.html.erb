<%= form_with(model: silence) do |form| %>
  <% if silence.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(silence.errors.count, "error") %> prohibited this silence from being saved:</h2>

      <ul>
        <% silence.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
