<%= form_with(model: caractere) do |form| %>
  <% if caractere.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(caractere.errors.count, "error") %> prohibited this caractere from being saved:</h2>

      <ul>
        <% caractere.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
