<%= form_with(model: left_hand_technique) do |form| %>
  <% if left_hand_technique.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(left_hand_technique.errors.count, "error") %> prohibited this left_hand_technique from being saved:</h2>

      <ul>
        <% left_hand_technique.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
