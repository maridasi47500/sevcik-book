<%= form_with(model: piece_type) do |form| %>
  <% if piece_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(piece_type.errors.count, "error") %> prohibited this piece_type from being saved:</h2>

      <ul>
        <% piece_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
