<%= form_with(model: musical_phrasing) do |form| %>
  <% if musical_phrasing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(musical_phrasing.errors.count, "error") %> prohibited this musical_phrasing from being saved:</h2>

      <ul>
        <% musical_phrasing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
