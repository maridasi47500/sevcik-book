<%= form_with(model: mystring) do |form| %>
  <% if mystring.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mystring.errors.count, "error") %> prohibited this mystring from being saved:</h2>

      <ul>
        <% mystring.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
