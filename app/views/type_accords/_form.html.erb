<%= form_with(model: type_accord) do |form| %>
  <% if type_accord.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(type_accord.errors.count, "error") %> prohibited this type_accord from being saved:</h2>

      <ul>
        <% type_accord.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
