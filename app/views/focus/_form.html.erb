<%= form_with(model: focu) do |form| %>
  <% if focu.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(focu.errors.count, "error") %> prohibited this focu from being saved:</h2>

      <ul>
        <% focu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
