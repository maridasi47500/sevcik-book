<%= form_with(model: modificateur) do |form| %>
  <% if modificateur.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(modificateur.errors.count, "error") %> prohibited this modificateur from being saved:</h2>

      <ul>
        <% modificateur.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
